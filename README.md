# Advanced Project in Digital Media (02830)

DTU course project. Work in progress, structure will change.

## Setup

Requires [uv](https://docs.astral.sh/uv/).

```bash
uv sync
cp .env.example .env   # then fill in your keys
uv run main.py
```

Add dependencies with `uv add <package>`, never pip.
